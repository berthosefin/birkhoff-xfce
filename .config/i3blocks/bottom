# i3blocks config file
#
# Please see man i3blocks for a complete reference!
# The man page is also hosted at http://vivien.github.io/i3blocks
#
# List of valid properties:
#
# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent

# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=$HOME/.i3scripts/$BLOCK_NAME
separator_block_width=40
markup=pango
separator=false
border_top=3
border_left=0
border_right=0
border_bottom=0

[ip]
command=ip address | awk '{if(NR==9) print $2}' | cut -d '/' -f 1
label=🖧
interval=120
border=#4c5f64
color=#4c5f64

[netdevice]
interval=30
border=#4c5f64
color=#4c5f64

[task]
interval=2
label=💪
border=#4c5f64
color=#4c5f64
align=left

[music]
interval=1
border=#4c5f64
color=#4c5f64

[space]
command=echo ' '
interval=once
